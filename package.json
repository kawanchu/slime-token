{
  "name": "slime-token",
  "version": "1.0.0",
  "private": true,
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "3333"
    }
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "rpc": "mkdir -p tmp/rpc && ganache-cli -h 0.0.0.0 --db tmp/rpc/ -i 5777 -d -m"
  },
  "dependencies": {
    "@nuxtjs/vuetify": "^0.4.2",
    "nuxt": "^1.0.0",
    "zeppelin-solidity": "^1.12.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-loader": "^1.7.1",
    "eslint-plugin-vue": "^4.0.0"
  }
}
